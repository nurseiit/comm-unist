package com.google.android.gms.internal;

import android.graphics.PointF;
import java.util.ArrayList;

public final class ie implements if {
    private float x = 0.0f;
    private float y = 0.0f;
    private final boolean zzbUN = false;

    public ie(float f, float f2, boolean z) {
    }

    public final void zza(ArrayList<PointF> arrayList, ArrayList<Float> arrayList2, float f, PointF pointF) {
        arrayList2.add(!arrayList2.isEmpty() ? (Float) arrayList2.get(arrayList2.size() - 1) : Float.valueOf(0.0f));
        PointF pointF2 = new PointF(this.x, this.y);
        arrayList.add(pointF2);
        pointF.set(pointF2);
    }
}
