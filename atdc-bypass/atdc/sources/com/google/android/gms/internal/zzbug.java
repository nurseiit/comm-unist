package com.google.android.gms.internal;

import java.util.concurrent.TimeUnit;

public interface zzbug<DP, DT> {
    long zza(DP dp, TimeUnit timeUnit);

    double zzb(DP dp, int i);

    int zzc(DP dp, int i);

    boolean zzd(DP dp, int i);

    zzbuh<DT> zztF();

    DT zzx(DP dp);

    String zzy(DP dp);
}
